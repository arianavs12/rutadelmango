import React from 'react'
import AppAppBar from "../components/AppAppBar";

const Tourism = (props) => {
  return (
        <div>
            <AppAppBar {...props} />
        </div>
  )
}

export default Tourism
